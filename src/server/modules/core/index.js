'use strict';
/**
 * Module core
 */

/**
 * Dependencies
 */
import connection from './connection';
import date from './date';
import date from './config';

/**
 * Module Export
 * @type {Object}
 */
module.exports = {
  connection: connection,
  date: date,
  config: config
};
